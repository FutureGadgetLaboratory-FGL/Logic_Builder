# 5. Input radius and calculate area and circumference of circle.
import math

r = int(input('Enter radius of circle:'))
print('Circumference of circle with radius {} is {}'.format(r, 2*math.pi*r))
